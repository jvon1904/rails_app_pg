<h1><%= @page.title %></h1>
<p><%= @page.body %></p>
<%= link_to "Edit", edit_path(@page) %> | 
<%= link_to "Back", pages_path %> | 
<%= link_to "Delete", @page, method: :delete, data: {confirm: "You sure about dat!?"} %>